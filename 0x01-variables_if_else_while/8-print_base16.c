#include <stdio.h>

/**
 * main - main block
 * Description: Print all numbers in base 16 in lowercase
 *
 * Return: 0
 */
int main(void)
{
	int c;

	for (c = 0; c < 10; c++)
	{
		putchar (c);
	}

	c = 'a'
	for (c = 'a'; c <= f; c++)
	{
		putchar (c);
	}
	putchar ('\n');
	return (0);
}
